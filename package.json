{
  "name": "cosa-nostra-website",
  "version": "1.0.0",
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "concurrently \"cd backend && node index.js\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-all": "concurrently \"cd backend && node index.js\" \"cd frontend && npm start\""
  }
}

